@page "/post/{slug}"
@rendermode InteractiveServer

<InitializeTimeZone />

@if (NotFound)
{
    <CustomPageTitle>@Slug</CustomPageTitle>

    <p>Nothing to see here</p>
}
else if (BlogPost == null)
{
    <CustomPageTitle>@Slug</CustomPageTitle>

    <p>Loading...</p>
}
else
{
    <CustomPageTitle>@BlogPost.Title</CustomPageTitle>

    <h1>
        @BlogPost.Title
    </h1>
    <p>
        <LocalTime DateTime="BlogPost.PubDate" />
    </p>
    <p>
        @BlogPost.Description
    </p>

    <hr />

    @if (!string.IsNullOrWhiteSpace(BlogPost.ContentHtml))
    {
        @((MarkupString)BlogPost.ContentHtml)
    }
}